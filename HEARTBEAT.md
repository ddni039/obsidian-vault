# HEARTBEAT.md

## 心跳期間可自動執行的事項（無需詢問）

在心跳間隔期間，可自動執行以下事項，無需另行詢問：

1. **整理 memory 文件** - 每日日记更新、清理过期内容、合并重要笔记
2. **检查项目状态** - `git status`、`git log --oneline -3`，保持工作区整洁
3. **更新文档** - 同步 SOUL.md、USER.md、MEMORY.md 等个人文件
4. **提交自己的改动** - 将确认的变更（skill 更新、文档修改等）自动 commit
5. **记忆维护**（每几天执行一次）
   - 阅读最近的 `memory/YYYY-MM-DD.md` 日记
   - 提取重要信息到 `MEMORY.md`
   - 清理过时内容（删除不再相关的旧笔记）
6. **每日健康檢查** - 每天 09:00 執行 `openclaw doctor --fix`（需使用 `oclaw` alias）

### 限制
- 不主动发起外部操作（发邮件、发消息）
- 不执行破坏性操作（删除文件、撤销 commit）
- 遇到不确定的情况，先询问再执行

---

## 信箱檢查任務

1. **檢查新郵件** - 使用 `gog gmail search 'newer_than:30m' --max 10`
2. **識別垃圾郵件** - 判斷常見垃圾郵件特徵（促銷、釣魚、可疑來源）
3. **清理垃圾郵件** - 使用 `gog gmail trash <id>` 刪除確認的垃圾郵件
4. **標記重要郵件** - 使用 `gog gmail modify <id> --add-label IMPORTANT` 標記重要郵件

### 安全規則
- 不確定的郵件不要刪除
- 釣魚郵件立即刪除（不要點擊任何連結）
- 銀行、政府郵件需特別注意

### 回報格式
執行完成後回報：
- 新郵件數量
- 已刪除垃圾郵件數量
- 發現的問題（如有）

---

## 異常報警機制

當 cron 任務失敗時，發送 [SECURITY_ALERT] 通知：
- exec 被拒絕時，立即推播提醒發送 `/approve`
- 使用 Telegram 群組 `open` 政策確保及時傳遞
