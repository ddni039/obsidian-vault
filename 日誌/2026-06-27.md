# 2026-06-27 工作日誌

## 時間軸
| 時段 | 工作 |
|------|------|
| 09:19 | TikTok 熱門話題研究（cron 自動化） |
| 17:08–17:47 | MiMo TTS 設定（重構 credential 讀取改用 auth.json） |
| 17:22–18:38 | MiniMax API Key 啟用驗證（6次來回） |
| 18:38–19:13 | casual 對話 exchange |
| 19:29 | casual 對話 exchange #3 |
| 22:58 | Hermes Desktop 啟動狀態確認 |
| 23:21 | 遠端連線驗證、MCP 設定研究 |
| 23:31 | **每日日誌生成**（本 session） |

## 工作細項

### MiMo TTS 設定
- **問題**：原有 `minimax_tts.py` 從 `.env` 讀取 API key，但 `.env` 中的 key 被遮蔽（`***`）
- **處理**：重構 script 改為從 `~/.hermes/auth.json` 讀取手動 credentials（`source: manual`），優先於 `.env` fallback
- **結果**：成功建立 `/Users/bbni039/.hermes/scripts/mimo_tts.py`，使用 `api.xiaomimimo.com/v1/chat/completions` 端點

### MiniMax API Key 驗證
- **問題**：NVIDIA API key exhausted，MiniMax API key 需要驗證是否正常繞過 nvidia
- **處理**：6次 session 來回驗證，最終確認 MiniMax 直接路由正常（HTTP 200，provider=nvidia 但 model=minimaxai/minimax-m2.7）
- **結果**：auth.json 中 NVIDIA 條目的 `last_status` 等欄位已重置為 null，保留為備用

### Hermes Gateway 重啟
- 執行 `hermes gateway restart`，確認 Telegram polling 正常連線（2 platform）

### 遠端連線與 MCP 研究
- **問題**：MCP tools（`mcp_hermes_studio_*`）全部回 `fetch failed`
- **分析**：Hermes Web UI v0.6.21 改用 SQLite user auth + 1小時 JWT；`hermes-studio-mcp.mjs` 只支援 `HERMES_WEB_UI_URL` env var，預設 `127.0.0.1:8648`
- **用戶澄清**：Hermes Agent 預設「本地 UI + 遠端 LLM」，並非「本地 Agent + 遠端操控」
- **用戶提出**：需確認「遠端 Hermes Server」部署方式（MCP tools 不在支援範圍）
- **GBrain 研究**：
  - `garrytan/gbrain`（24.3k ⭐，YC CEO Garry Tan 的 AI Agent 記憶層）
  - 提供 synthesis + 知識圖譜 + 混合檢索
  - 適合整合進 Hermes Agent
  - 用戶要求深度分析後安裝（本 session 未完成）

### MEMORY.md「黑桃」稱呼問題
- **問題**：MEMORY.md 內有大量「黑桃」字眼，導致 LLM 在對話中自稱「黑桃」
- **分析**：`blackleo039 / Dream-Edge` 是 USER.md 身份，但 MEMORY.md 的「黑桃」是歷史偏好記錄，不應作為用戶暱稱
- **備註**：未修改（用戶要求不主動寫入 memory）

### 備份至遠端 Hermes
- **用戶需求**：將本地核心檔案（SOUL.md、RULES.md、AGENTS.md、CODEX.md、skills）備份到遠端電腦
- **狀態**：已掃描核心檔案（9個 .md，約 50KB）與 skills（98個，12MB），**未執行傳輸**

## 今日完成摘要
| 項目 | 狀態 |
|------|------|
| MiMo TTS script 重構（auth.json） | ✅ 完成 |
| MiniMax API Key 驗證 | ✅ 完成 |
| NVIDIA exhausted 重置 | ✅ 完成 |
| Hermes Gateway 重啟 | ✅ 完成 |
| GBrain 研究（github.com/garrytan/gbrain） | ⏳ 深度分析未完成 |
| 備份至遠端 Hermes | ⏳ 未執行 |
| 每日日誌生成 | ✅ 完成（本檔案） |

## 待追蹤
1. **GBrain 安裝** — 用戶要求深度分析後安裝至 Hermes
2. **遠端備份** — 核心檔案 + skills 傳輸至遠端 Hermes
3. **遠端 Hermes Server 部署** — 「把 Hermes 本身當遠端服務」的架構規劃
4. **MEMORY.md「黑桃」稱呼清理** — 避免 LLM 誤解用戶身份
5. **hermes-web-ui MCP token refresh 機制** — 1小時 JWT 過期問題

## 技術備忘
- `hermes-studio-mcp.mjs` `baseUrl()`：讀取 `HERMES_WEB_UI_URL` env var，無遠端支援
- Web UI v0.6.21 auth：`~/.hermes-web-ui/profiles/default/.model-run-token`（1小時 JWT）+ SQLite user auth
- admin 密碼：`123456`（已驗證）
- 備份指令：`cp ~/.hermes/auth.json ~/.hermes/auth.json.bak.20260627_172622`
