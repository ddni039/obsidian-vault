# 2026-06-11 工作日誌

## 時間軸
| 時段 | 工作 |
|------|------|
| 14:16–15:07 | Phase5 架構守衛部署 + arch_guard.py 双重審計 |
| 15:10–16:44 | HERMES 四人格串流 SOP v1.1.0 撰寫與固化 |
| 17:51–19:37 | Kanban Service 測試實驗（Jest ESM / uuid 修復） |
| 17:55–19:37 | Subagent 可驗證 handle 設計討論 |
| 19:24–20:16 | Codex CLI 研究 + PDF 生成（Claude Code × Hermes 串連架構） |

## 工作細項

### 1. Phase5 架構守衛部署（14:16–15:07）

**問題：** MEMORY.md 容量防呆機制需要自動化審計。

**處理：**
- 完成 `arch_guard.py` 双重審計官部署
  - 靜態資產審計：`core/SOUL.md` Phase5 鋼印完整性
  - 動態快取審計：`memories/MEMORY.md` 容量防呆 + RUNTIME 零殘留
- 確立 `[CORE]`（永久靜態層）與 `[RUNTIME]`（運行期快取）職責邊界
- 終局驗收：`make arch-guard` 退出碼 0（PASS）

**結果：** ✅ arch_guard.py 已部署，雙重審計常態化

---

### 2. HERMES 四人格串流 SOP v1.1.0（15:10–16:44）

**問題：** Sub-agent 協作流程需要制度化，防止「60% 半成品」問題。

**處理：**
- 撰寫並固化 `~/.hermes/skills/personas/personas-workflow/SOP.md`（234行，v1.1.0）
- 新增核心定律：
  - **60% 工匠定律**：沒有御史鐵律，工匠只交 60% 半成品
  - **NFR 前置原則**：文謀必須在 plan 階段寫入非功能需求
  - **死守清單原則**：御史用逐項核查取代隨機盲測
  - **主控固化原則**：主控每次補破網都應該變成系統基因
- 文謀 prompt 新增 NFR 強制區塊（安全/測試/執行期/記憶體）
- 御史 prompt 新增四大死穴核查清單
- 確立 Step 4 主控固化流程

**結果：** ✅ SOP v1.1.0 固化至 skills，目後新任務將以新流程執行

---

### 3. Kanban Service 測試實驗（17:51–19:37）

**問題：** Jest 無法處理 `uuid` ESM 模組，導致測試 suite 崩潰。

**處理：**
- 建立 `/tmp/hermes-kanban/` 下的 Repository + Service 單元測試
- 寫入 `jest.config.js` + `tests/__mocks__/uuid.js` CJS stub
- 修復 `transformIgnorePatterns` 配置（uuid 需明確排除）

**結果：** ⚠️ 修復中，ESM/CJS 混用問題複雜，MVP 階段暫緩

---

### 4. Subagent 可驗證 Handle 設計（17:55–19:37）

**問題：** Sub-agent 的文字摘要沒有物理簽章，無法防止幻覺/失敗卻聲稱完成。

**處理：**
- 提出「Trust, but Verify」設計原則
- 強制要求工匠返回可驗證 artifact：
  - 檔案修改 → 絕對路徑 + commit SHA
  - PR 建立 → PR URL 或 number
  - API 呼叫 → response ID / status code
- 主控收到 handle 後需主動查證，不只靠摘要

**結果：** ✅ 設計原則確立，待後續整合至 delegate_task 流程

---

### 5. Codex CLI 研究 + Architecture PDF（19:24–20:16）

**問題：** 需要系統性整理 Claude Code CLI 與 Codex CLI 如何與 Hermes Agent 串連。

**處理：**
- 研究 Codex CLI 兩種使用模式：
  - **LLM Provider**：作為對話模型（gpt-5.5），OAuth 無痛整合
  - **Autonomous Agent**：獨立 CLI，需透過 terminal 呼叫
- 確認 delegate_task 的 provider 清單（`openrouter, nous, zai, kimi-coding, minimax`），Codex 不在可直接delegate，需終端呼叫
- 生成架構 PDF（6章節）：三者定位差異、整合方式、Subagent 分工、並行分工、Pitfalls、推薦 Workflow

**結果：** ✅ PDF 已生成至 `~/Downloads/hermes-claude-codex-architecture.pdf`

---

## 今日完成摘要

| 項目 | 狀態 |
|------|------|
| arch_guard.py 雙重審計部署 | ✅ |
| HERMES 四人格 SOP v1.1.0 固化 | ✅ |
| Kanban Jest 測試實驗 | ⚠️ 修復中（ESM/uuid） |
| Subagent 可驗證 handle 設計原則 | ✅ |
| Claude Code × Codex × Hermes 架構 PDF | ✅ |

## 待追蹤

- [ ] Kanban uuid ESM 問題完整修復（需專門處理 ESM/CJS 隔離）
- [ ] Codex CLI 安裝（`npm install -g @openai/codex`）
- [ ] Subagent 可驗證 handle 整合進 delegate_task 流程
- [ ] TTS 音色調整（MiniMax Warm Girl 音色測試）

## Git Commit（Vault）

```
2e182ce docs: add Hermes Studio (hermes-web-ui) wiki article
```
